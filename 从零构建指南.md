# 快递网点配送路径规划 — 从零构建指南

> 适用：数据结构与算法课设学生，有 C++ 基础，零 Qt 经验
> 目标：从空白文件夹开始，一步步写出完整的快递路径规划系统
> 预计：6 节课，每节 2-3 小时

---

## 目录

1. [前置准备](#一前置准备)
2. [DynArray 动态数组](#二dynarrayt--动态数组)
3. [HashMap 哈希表](#三hashmapv--开放地址哈希表)
4. [MinHeap 最小堆 + Queue 队列](#四minheaptcmp--二叉最小堆--queuet--队列)
5. [Graph 有向图邻接表](#五graph--有向图邻接表)
6. [Dijkstra 最短路径 + 拓扑排序](#六dijkstra-最短路径--拓扑排序)
7. [OrderManager 订单 + 文件读写](#七ordermanager-订单管理--文件读写)
8. [Qt GUI 图形界面](#八qt-gui-图形界面)
9. [数据 + 构建 + 编译运行](#九数据文件--构建配置--编译运行)
10. [算法详解附录](#十算法详解附录)
11. [答辩准备](#十一答辩准备)

---

## 一、前置准备

### 1.1 我们要做什么？

```
┌──────────────────────────────────────────────┐
│       快递网点配送路径规划系统                    │
│                                              │
│  25 个中国城市，124 条有向边                     │
│  北京 ──→ 天津（1.5h / 60元）                  │
│  北京 ──→ 石家庄（3h / 90元）                   │
│    ...                                       │
│                                              │
│  功能：                                       │
│  1. 输入起点 → 到所有城市的最短耗时               │
│  2. 输入起点+终点 → 最低费用路径                  │
│  3. 批量导入订单 → 自动规划全部路线               │
│  4. 检测城市间是否有循环依赖（拓扑排序）            │
│  5. 画布上高亮路径，鼠标悬停看节点信息              │
└──────────────────────────────────────────────┘
```

### 1.2 课程红线

> ⚠️ **禁止使用 STL 容器**：`vector`、`list`、`queue`、`stack`、`map`、`priority_queue` 全部不能用

意味着：动态数组、哈希表、堆、队列 —— 统统自己写。

### 1.3 我们需要什么头文件？

**核心算法层（Containers → Graph → Dijkstra → OrderManager）用的标准库**：

| 头文件 | 哪里用 | 用途 |
|--------|--------|------|
| `<string>` | Graph.h | Node 的城市名和地址 |
| `<iostream>` | Graph.cpp / Dijkstra.cpp | 打印日志 |
| `<fstream>` | OrderManager.cpp | 读写 txt 文件 |
| `<sstream>` | OrderManager.cpp | 字符串流解析 |

一共 **4 个**，都是 C++98 最基础的东西。核心容器 `Containers.h` **零外部依赖**。

### 1.4 安装 Qt

1. 下载 Qt Online Installer：https://www.qt.io/download
2. 安装时勾选 **Qt 6.x.x** → `MSVC 2022 64-bit` 或 `MinGW 64-bit`
3. 安装 **Qt Creator**（推荐，免去 VS Code 的复杂配置）

### 1.5 文件夹结构

```
ExpressRouting/
├── Containers.h          # 4 个自实现容器（零外部依赖）
├── Graph.h / Graph.cpp     # 有向图邻接表
├── Dijkstra.h / Dijkstra.cpp  # 最短路径 + 拓扑排序
├── OrderManager.h / OrderManager.cpp  # 订单 + 文件读写
├── MainWindow.h / MainWindow.cpp      # Qt 界面
├── main.cpp              # 程序入口
├── CMakeLists.txt        # 构建配置
├── data/
│   ├── network.txt       # 25 城市路网
│   └── orders.txt        # 5 条测试订单
└── .vscode/              # VS Code 配置（可选）
```

### 1.6 开发顺序（重要！）

```
容器层 → 图层 → 算法层 → 业务层 → GUI层
```

**不要从 GUI 开始写。** 每一层写完立刻测试，确保无 bug 再往下走。

---

## 二、DynArray<T> — 动态数组

### 2.1 为什么需要它？

整个项目到处需要"可变长数组"：
- 图的邻接表：每个节点存一个出边列表
- Dijkstra 的路径：存一串节点编号
- MinHeap 的底层：用数组存完全二叉树

所以 DynArray 是**地基**。地基不稳，全楼倒塌。

### 2.2 原理

```
初始：                                扩容后：
_d = nullptr                         _d = [A][B][C][?]
_sz = 0                              _sz = 3
_cap = 0                             _cap = 4

push_back("D"):                      push_back("E"):
  _sz(3) < _cap(4) → 直接放 [D]        _sz(4) == _cap(4) → 满了！
                                       new T[8]（2 倍）
                                       拷贝 4 个旧元素过去
void grow() {                          delete[] 旧数组
  新容量 = _cap*2（初始给 4）           再放 E
  申请新数组 new T[新容量]
  循环拷贝 nd[i] = _d[i]
  删旧数组，更新指针和容量
}
```

**为什么扩容是 2 倍？**

push n 次，扩容发生在第 4、8、16、... 次。总拷贝次数 = 4+8+16+...+n/2 ≈ n。均摊到每次 push 上 = O(1)。

如果每次只加 10 个位置，总拷贝 = 10+20+30+...+n = O(n²)。

### 2.3 完整代码

创建 `Containers.h`：

```cpp
#pragma once
// ============================================================================
// Containers.h — 自实现容器库（替代 STL）
// ============================================================================
// 四个基础容器：DynArray / HashMap / MinHeap / Queue
// 零外部依赖，不使用 std::move / std::swap / if constexpr
// ============================================================================

// ============================================================================
// DynArray<T> — 动态数组
// ============================================================================
// 核心操作：push_back 均摊 O(1)。满了扩容 2 倍。
// 拷贝：逐元素深拷贝（不能 memcpy，因为 T 可能是 string）
// ============================================================================
template<typename T>
class DynArray {
    T* _d = nullptr;   // 堆数组指针
    int _sz = 0;       // 当前元素数
    int _cap = 0;      // 容量

    void grow() {
        int nc = _cap ? _cap * 2 : 4;   // 初始 4 个槽
        T* nd = new T[nc];
        for (int i = 0; i < _sz; ++i) nd[i] = _d[i];  // 逐元素赋值
        delete[] _d; _d = nd; _cap = nc;
    }

public:
    DynArray() = default;

    // 拷贝：逐元素 push_back（深拷贝）
    DynArray(const DynArray& o) {
        for (int i = 0; i < o._sz; ++i) push_back(o._d[i]);
    }

    ~DynArray() { delete[] _d; }

    // 赋值：先清空再拷贝（防止 a=a 自赋值）
    DynArray& operator=(const DynArray& o) {
        if (this == &o) return *this;
        delete[] _d; _d = nullptr; _sz = _cap = 0;
        for (int i = 0; i < o._sz; ++i) push_back(o._d[i]);
        return *this;
    }

    // ---- 增删 ----
    void push_back(const T& v) {
        if (_sz == _cap) grow();
        _d[_sz++] = v;
    }
    void pop_back()             { if (_sz > 0) --_sz; }

    // 删索引 i，后面元素前移
    void erase(int i) {
        for (; i < _sz - 1; ++i) _d[i] = _d[i + 1]; --_sz;
    }

    // 删除第一个匹配的元素
    template<typename P> bool remove_first(P pred) {
        for (int i = 0; i < _sz; ++i) if (pred(_d[i])) { erase(i); return true; }
        return false;
    }

    // 删除所有匹配元素（删了不 +i，因为下个元素移到此位置）
    template<typename P> void remove_all(P pred) {
        for (int i = 0; i < _sz;) { if (pred(_d[i])) erase(i); else ++i; }
    }

    // ---- 访问 ----
    T&       operator[](int i)       { return _d[i]; }
    const T& operator[](int i) const { return _d[i]; }
    T&       back()                  { return _d[_sz - 1]; }

    int  size()  const { return _sz; }
    bool empty() const { return _sz == 0; }
    void clear()       { _sz = 0; }    // 不释放内存，可快速重用

    // 迭代器（支持 for (auto& x : arr) ...）
    T* begin()       { return _d; }
    T* end()         { return _d + _sz; }
    const T* begin() const { return _d; }
    const T* end()   const { return _d + _sz; }
};
```

### 2.4 测试

```cpp
DynArray<int> a;
for (int i = 0; i < 10; ++i) a.push_back(i * 10);

// 测试访问
cout << a[3] << "\n";           // 应输出 30

// 测试拷贝（必须是深拷贝）
DynArray<int> b = a;
b[0] = 999;
cout << a[0] << "\n";           // 应输出 0，不是 999

// 测试 erase
a.erase(3);
cout << a[3] << "\n";           // 应输出 40（原来的 30 被删了）
```

---

## 三、HashMap<V> — 开放地址哈希表

### 3.1 为什么键固定为 int？

项目中所有哈希表都用 int 键：
- `HashMap<Node>` — 节点编号 → 节点信息
- `HashMap<DynArray<Edge>>` — 节点编号 → 出边列表
- `HashMap<double>` — 节点编号 → 距离
- `HashMap<int>` — 节点编号 → 前驱节点编号
- `HashMap<bool>` — 节点编号 → 集合中是否存在

键只有 int 这一种。所以把键类型固定为 int，代码少一个模板参数，更简单。

### 3.2 核心原理

```
哈希表 = 数组 + 哈希函数

一、哈希函数（乘法哈希）：
  h(k) = (k × 2654435761) mod 表容量
  2654435761 = 2³² × (√5-1)/2 = 黄金比例的倒数 × 2³²
  效果：把 1,2,3... 连续整数均匀散列到各个槽

二、冲突解决（开放地址 + 线性探测）：
  k₁=5 → slot 3，k₂=13 → 也算出 slot 3（冲突！）
  → 看 slot 4，空 → 放 slot 4
  就像找停车位，首选被占了就往前找一个

三、惰性删除（只打标记不清空）：
  为什么不能直接清空槽？
  例：key=5放slot3, key=13冲突后放slot4
  删了slot3的key=5 → 找key=13时看slot3是空的 → "不存在"（错！13在slot4）
  打 del=true 标记 → 找13时跳过标记槽 → 找到slot4 ✓

四、负载因子 > 0.5 扩容：
  有效元素数 / 容量 > 0.5 → 扩容 2 倍，所有元素重新哈希
  为什么 0.5？开放地址下负载太高探测链会很长，0.5 保证平均探测 ≤ 2 次
```

### 3.3 完整代码

追加到 `Containers.h`：

```cpp
// ============================================================================
// HashMap<V> — 开放地址哈希表（键固定为 int）
// ============================================================================
// 四个核心设计：乘法哈希 + 线性探测 + 惰性删除 + 0.5 负载扩容
// ============================================================================
template<typename V>
class HashMap {
    struct Slot {
        int  key;
        V    val;
        bool used = false;   // 被占用过吗
        bool del  = false;   // 被惰性删除了吗
    };

    Slot* _s = nullptr;
    int   _cap = 0;
    int   _sz = 0;

    // 乘法哈希
    unsigned h(int k) const {
        return (unsigned)k * 2654435761u % (unsigned)_cap;
    }

    // 扩容到 nc，所有有效元素重新哈希
    void rehash(int nc) {
        Slot* old = _s; int oc = _cap;
        _s = new Slot[nc]();    // () 让 used/del 全 false
        _cap = nc; _sz = 0;
        for (int i = 0; i < oc; ++i)
            if (old[i].used && !old[i].del)
                (*this)[old[i].key] = old[i].val;  // operator[] 搬到新表
        delete[] old;
    }

public:
    HashMap(int c = 16) : _s(new Slot[c]()), _cap(c), _sz(0) {}

    HashMap(const HashMap& o)
        : _s(new Slot[o._cap]()), _cap(o._cap), _sz(o._sz) {
        for (int i = 0; i < _cap; ++i) _s[i] = o._s[i];
    }

    ~HashMap() { delete[] _s; }

    HashMap& operator=(const HashMap& o) {
        if (this == &o) return *this;
        delete[] _s;
        _s = new Slot[o._cap](); _cap = o._cap; _sz = o._sz;
        for (int i = 0; i < _cap; ++i) _s[i] = o._s[i];
        return *this;
    }

    // ---- operator[]：查找或插入 ----
    // 有则返回引用，无则插入默认值再返回引用
    V& operator[](int k) {
        if (_sz * 2 >= _cap) rehash(_cap * 2);  // 负载 ≥ 0.5

        unsigned i = h(k);
        int fd = -1;      // 遇到的第一个 del 槽（可复用）

        while (_s[i].used) {
            if (_s[i].del) {
                if (fd < 0) fd = (int)i;          // 记下
            } else if (_s[i].key == k) {
                return _s[i].val;                 // 找到了
            }
            i = (i + 1) % (unsigned)_cap;         // 线性探测
        }

        int ins = (fd >= 0) ? fd : (int)i;        // 优先复用 del 槽
        _s[ins] = { k, V{}, true, false };         // V{} = 默认值
        ++_sz;
        return _s[ins].val;
    }

    void set(int k, const V& v) { (*this)[k] = v; }

    // ---- find：查找，返回指针 ----
    V* find(int k) {
        unsigned i = h(k), s = i;
        while (_s[i].used) {
            if (!_s[i].del && _s[i].key == k) return &_s[i].val;
            if ((i = (i + 1) % (unsigned)_cap) == s) break;  // 绕一圈
        }
        return nullptr;
    }
    const V* find(int k) const { /* 同上 */ ... }

    bool contains(int k) const { return find(k) != nullptr; }

    // ---- erase：惰性删除 ----
    bool erase(int k) {
        unsigned i = h(k), s = i;
        while (_s[i].used) {
            if (!_s[i].del && _s[i].key == k) { _s[i].del = true; --_sz; return true; }
            if ((i = (i + 1) % (unsigned)_cap) == s) break;
        }
        return false;
    }

    int  size()  const { return _sz; }
    bool empty() const { return _sz == 0; }

    void clear() {
        for (int i = 0; i < _cap; ++i) _s[i].used = _s[i].del = false;
        _sz = 0;
    }

    // forEach：遍历所有有效元素
    template<typename F> void forEach(F f) {
        for (int i = 0; i < _cap; ++i)
            if (_s[i].used && !_s[i].del) f(_s[i].key, _s[i].val);
    }
    template<typename F> void forEach(F f) const {
        for (int i = 0; i < _cap; ++i)
            if (_s[i].used && !_s[i].del) f(_s[i].key, _s[i].val);
    }
};
```

> 注意：`find(const K& k) const` 的实现和非常量版一模一样，为节省篇幅上面省略了。实际代码中包含两版本。

### 3.4 测试

```cpp
HashMap<double> dist;   // 键=int，值=double，表示距离
dist[1] = 5.5;
dist[2] = 3.2;

double* v = dist.find(1);
cout << *v << "\n";           // 5.5

cout << dist[3] << "\n";     // 0（自动创建，默认值）

dist.erase(2);
cout << dist.contains(2);    // 0 (false)
```

---

## 四、MinHeap<T,Cmp> — 二叉最小堆 + Queue<T> — 队列

### 4.1 MinHeap 原理

```
堆 = 完全二叉树，用数组存储。
最小堆 = 父节点 ≤ 子节点，堆顶就是最小值。

索引规则（不用指针，用算术）：
  左孩子 = 2i + 1
  右孩子 = 2i + 2
  父节点 = (i-1) / 2

例：[2, 5, 8, 9, 7]
      2          ← 堆顶
    /   \
   5     8
  / \
 9   7

push(3)：
  放末尾：[2,5,8,9,7,3]
  元素3(idx=5)上浮：父idx=2 arr[2]=8 > 3 → 交换 [2,5,3,9,7,8]
  继续上浮：父idx=0 arr[0]=2 < 3 → 停止

pop()：
  取堆顶2，末尾8移到堆顶：[8,5,3,9,7]
  下沉：左5右3，3更小且3<8 → 交换 [3,5,8,9,7]
  继续：左9右7，7更小但7>3 → 停止
```

**在 Dijkstra 中做优先队列（惰性删除）**：

```
正常做法：找到更短路径 → 在堆中找到旧记录 → 更新 → O(n) 太慢
我们的做法：直接 push 新记录 (7,node5)，旧记录 (10,node5) 留在堆里
          每次 pop 时在外层检查：堆顶的距离 > 已知最短距离？→ 跳过
```

### 4.2 代码

追加到 `Containers.h`：

```cpp
// ============================================================================
// MinHeap<T,Cmp> — 二叉最小堆
// ============================================================================
template<typename T>
struct HeapLess { bool operator()(const T& a, const T& b) const { return a < b; } };

template<typename T, typename Cmp = HeapLess<T>>
class MinHeap {
    DynArray<T> _d;
    Cmp         _c;

    // 上浮：和父节点比，小则交换
    void up(int i) {
        while (i > 0) {
            int p = (i - 1) / 2;
            if (_c(_d[i], _d[p])) {
                T t = _d[i]; _d[i] = _d[p]; _d[p] = t;   // 手动交换
                i = p;
            } else break;
        }
    }

    // 下沉：和左右孩子中较小的比，大则交换
    void down(int i) {
        int n = _d.size();
        while (true) {
            int b = i, l = 2*i+1, r = 2*i+2;
            if (l < n && _c(_d[l], _d[b])) b = l;
            if (r < n && _c(_d[r], _d[b])) b = r;
            if (b == i) break;
            T t = _d[i]; _d[i] = _d[b]; _d[b] = t;        // 手动交换
            i = b;
        }
    }

public:
    bool      empty() const { return _d.empty(); }
    const T&  top()   const { return _d[0]; }

    void push(const T& v) { _d.push_back(v); up(_d.size() - 1); }

    void pop() {
        if (_d.empty()) return;
        _d[0] = _d.back();       // 末尾元素放堆顶
        _d.pop_back();
        if (!_d.empty()) down(0);
    }
};
```

### 4.3 Queue

```
FIFO：队尾进、队头出
实现：DynArray 存数据 + head 下标标记队头
缺点：head 只增不减，前面空间不回收（拓扑排序中队列寿命很短，无影响）
```

```cpp
// ============================================================================
// Queue<T> — 队列
// ============================================================================
template<typename T>
class Queue {
    DynArray<T> _d;
    int _h = 0;

public:
    bool empty() const    { return _h >= _d.size(); }
    T&   front()          { return _d[_h]; }
    void push(const T& v) { _d.push_back(v); }
    void pop()            { if (!empty()) ++_h; }
    void clear()          { _d.clear(); _h = 0; }
};
```

### 4.4 测试

```cpp
MinHeap<int> h;
h.push(5); h.push(2); h.push(8); h.push(1);
while (!h.empty()) { cout << h.top() << " "; h.pop(); }
// 输出：1 2 5 8
```

✅ 容器层完成！现在我们有：
- `DynArray<T>` — 动态数组（~70 行）
- `HashMap<V>` — 哈希表（~100 行）
- `MinHeap<T>` — 最小堆（~45 行）
- `Queue<T>` — 队列（~15 行）
- **全部零外部依赖**，一个标准库头文件都不需要

---

## 五、Graph — 有向图邻接表

### 5.1 数据结构设计

```
存储选择：
┌──────────┬───────────────────┬─────────────────────┐
│          │ 邻接矩阵           │ 邻接表（我们选的）    │
├──────────┼───────────────────┼─────────────────────┤
│ 结构     │ double[V][V]      │ HashMap + DynArray  │
│ 空间     │ O(V²) = 25² = 625 │ O(V+E) = 25+124     │
│ 遍历出边 │ O(V) 扫整行        │ O(degree) 只看有边的 │
│ 删除节点 │ O(V)              │ O(E)                │
└──────────┴───────────────────┴─────────────────────┘

我们的图：25节点，124条有向边，密度 124/(25×24)≈20% → 稀疏图 → 邻接表
```

### 5.2 Graph.h

创建 `Graph.h`：

```cpp
#pragma once
#include "Containers.h"
#include <string>

// 节点：编号 + 名称 + 地址 + 经纬度（画图用）
struct Node {
    int id = 0;
    std::string name;
    std::string address;
    double lon = 0, lat = 0;

    Node() = default;
    Node(int i, const std::string& n, const std::string& a,
         double lo = 0, double la = 0)
        : id(i), name(n), address(a), lon(lo), lat(la) {}
};

// 有向边（双权）
struct Edge {
    int from = 0, to = 0;
    double time = 0;   // 运输耗时（小时）
    double cost = 0;   // 运输费用（元）

    Edge() = default;
    Edge(int f, int t, double ti, double co)
        : from(f), to(t), time(ti), cost(co) {}
};

class Graph {
public:
    // 节点 CRUD
    bool  addNode(const Node& n);
    bool  deleteNode(int id);
    bool  updateNode(int id, const Node& n);
    Node*       findNode(int id);
    const Node* findNode(int id) const;

    // 边 CRUD
    bool  addEdge(const Edge& e);
    bool  deleteEdge(int from, int to);

    // 查询
    const DynArray<Edge>& getNeighbors(int id) const;
    DynArray<int> getAllNodeIds() const;
    bool hasNode(int id) const;
    int  nodeCount() const;
    int  edgeCount() const;
    void clear();

private:
    HashMap<Node>           nodes_;
    HashMap<DynArray<Edge>> adj_;
    static const DynArray<Edge> emptyNeighbors_;
};
```

### 5.3 Graph.cpp

创建 `Graph.cpp`：

```cpp
#include "Graph.h"
#include <iostream>

const DynArray<Edge> Graph::emptyNeighbors_;

// ---- 节点 CRUD ----
bool Graph::addNode(const Node& n) {
    if (n.name.empty()) return false;
    if (nodes_.contains(n.id)) return false;
    nodes_.set(n.id, n);
    adj_[n.id];                    // 初始化空邻接表
    return true;
}

bool Graph::deleteNode(int id) {
    if (!nodes_.contains(id)) return false;
    nodes_.erase(id);
    adj_.erase(id);
    // 删除所有其他节点指向此节点的入边 O(E)
    adj_.forEach([id](int, DynArray<Edge>& es) {
        es.remove_all([id](const Edge& e) { return e.to == id; });
    });
    return true;
}

// ... updateNode / findNode（完整代码见项目文件）

// ---- 边 CRUD ----
bool Graph::addEdge(const Edge& e) {
    if (!nodes_.contains(e.from) || !nodes_.contains(e.to)) return false;
    if (e.from == e.to) return false;      // 不自环
    if (e.time < 0 || e.cost < 0) return false;  // 权重非负

    DynArray<Edge>& es = adj_[e.from];
    for (int i = 0; i < es.size(); ++i)    // 查重
        if (es[i].to == e.to) return false;
    es.push_back(e);
    return true;
}

bool Graph::deleteEdge(int from, int to) {
    DynArray<Edge>* ep = adj_.find(from);
    if (!ep) return false;
    return ep->remove_first([to](const Edge& e) { return e.to == to; });
}

// ---- 查询 ----
const DynArray<Edge>& Graph::getNeighbors(int id) const {
    const DynArray<Edge>* p = adj_.find(id);
    return p ? *p : emptyNeighbors_;
}

DynArray<int> Graph::getAllNodeIds() const {
    DynArray<int> ids;
    nodes_.forEach([&](int id, const Node&) { ids.push_back(id); });
    return ids;
}
```

> 完整代码见 ExpressRouting/Graph.cpp。

---

## 六、Dijkstra 最短路径 + 拓扑排序

### 6.1 Dijkstra 算法原理

```
目标：求从起点 src 出发，到所有节点的最短路径。

★ 前提条件：所有边的权重 ≥ 0（如果权重有负数，Dijkstra 就失效了）

直观理解（贪心）：
  1. 一开始，只确定 src 到自己距离 = 0，其他节点距离 = ∞
  2. 每次从"未确定"的节点中选出距离最小的 u
  3. 用 u 去"松弛"它的所有邻居 v：
     如果 dist[u] + weight(u→v) < dist[v]，更新 dist[v]
  4. 标记 u 为"已确定"，回到步骤 2

为什么每次选最小值就能保证正确？
  因为所有边权 ≥ 0，最小值节点不可能通过其他未确定节点再被缩短。
  （其他未确定节点的距离都 ≥ dist[u]，加上正边权只会更大）

朴素版 vs 堆优化版：
  朴素：每轮扫 V 个找最小 → O(V²)
  堆优化：用最小堆维护候选 → 取最小 O(log V) → O((V+E)logV)
```

### 6.2 惰性删除

```
为什么需要惰性删除？
  正常做法：堆中已有 (dist=10, node=5)，找到更短路径 dist=7
    → 需要在堆中定位 node=5 → 把 10 改成 7（decrease-key）
    → 二叉堆不支持快速定位，需要 O(n) 扫描

惰性删除做法：
  直接 push 新记录 (7, node5)。旧记录 (10, node5) 留在堆里。
  pop 时检查：堆顶记录的 dist > 当前已知的 dist[node]？
    → 是：旧记录，跳过
    → 否：该处理了

代价：堆中可能有旧垃圾，但每条边最多产生一个垃圾。
堆大小从 O(V) 变成 O(E)，logE 仍然有效。
```

### 6.3 路径回溯

```
Dijkstra 过程中维护 prev[] 数组：
  prev[v] = u 表示"到达 v 的最短路径中，v 的前驱是 u"

回溯：从终点 dst 倒着走
  cur = dst
  while cur != -1:
    路径.push(cur)
    cur = prev[cur]    // 往前找
翻转路径 → [src, ..., dst]
```

### 6.4 双权处理

| | shortestTimeFrom | cheapestPath |
|---|---|---|
| 主优化目标 | time（耗时） | cost（费用） |
| 辅助记录 | cost（顺带算） | time（顺带算） |
| 比较语句 | `nd = *dp + e.time` | `nd = *dp + e.cost` |

两条 Dijkstra 结构完全相同，只有被比较的权重不同。

### 6.5 Dijkstra.h

创建 `Dijkstra.h`：

```cpp
#pragma once
#include "Graph.h"

struct PathResult {
    DynArray<int> path;
    double totalTime = 0;
    double totalCost = 0;
    bool   reachable = false;
};

struct TopoResult {
    bool          hasCycle = false;
    DynArray<int> order;
    DynArray<int> cycleNodes;
};

class Dijkstra {
public:
    // 单源最短耗时：返回 HashMap<节点编号 → 路径结果>
    static HashMap<PathResult> shortestTimeFrom(const Graph& g, int src);

    // 两点最低费用
    static PathResult cheapestPath(const Graph& g, int src, int dst);
};

class TopoSort {
public:
    // Kahn 拓扑排序（对 ids 子集）
    static TopoResult sort(const Graph& g, const DynArray<int>& ids);
};
```

### 6.6 Dijkstra.cpp

创建 `Dijkstra.cpp`：

```cpp
#include "Dijkstra.h"
#include <iostream>

// INF 用一个极大的 double 值代替"无穷远"
static const double INF = 1e18;

// 堆中元素
struct DE { double d; int n; };
struct DECmp { bool operator()(const DE& a, const DE& b) const { return a.d < b.d; } };

// 路径回溯：从 dst 沿 prev[] 倒推回 src，再翻转
static DynArray<int> tracePath(const HashMap<int>& prev, int dst) {
    DynArray<int> p;
    for (int cur = dst; cur != -1; ) {
        p.push_back(cur);
        const int* pp = prev.find(cur);
        cur = pp ? *pp : -1;
    }
    for (int l = 0, r = p.size() - 1; l < r; ++l, --r) {
        int t = p[l]; p[l] = p[r]; p[r] = t;
    }
    return p;
}

// 初始化 dist 数组
static void initMaps(const DynArray<int>& ids,
                     HashMap<double>& distA, HashMap<double>& distB,
                     HashMap<int>& prev) {
    for (int i = 0; i < ids.size(); ++i) {
        distA[ids[i]] = INF;
        distB[ids[i]] = INF;
        prev[ids[i]] = -1;
    }
}

// ================================================================
// 单源最短耗时
// ================================================================
HashMap<PathResult> Dijkstra::shortestTimeFrom(const Graph& g, int src) {
    if (!g.hasNode(src)) return {};

    DynArray<int> ids = g.getAllNodeIds();
    HashMap<double> dist;    // 最短耗时
    HashMap<double> cost;    // 对应费用（辅助）
    HashMap<int>    prev;    // 前驱
    initMaps(ids, dist, cost, prev);
    dist[src] = cost[src] = 0;

    MinHeap<DE, DECmp> pq;
    pq.push({ 0.0, src });

    while (!pq.empty()) {
        DE top = pq.top(); pq.pop();

        // ★ 惰性删除：堆顶距离 > 已知最短 → 旧记录，跳过
        double* dp = dist.find(top.n);
        if (!dp || top.d > *dp) continue;

        // 松弛所有出边
        for (const Edge& e : g.getNeighbors(top.n)) {
            double nd = *dp + e.time;          // 以 time 为主权重
            double* dv = dist.find(e.to);
            if (dv && nd < *dv) {              // 找到更短路径
                *dv = nd;
                *cost.find(e.to) = *cost.find(top.n) + e.cost;  // 顺带算费用
                prev[e.to] = top.n;
                pq.push({ nd, e.to });         // ★ 只 push 新记录
            }
        }
    }

    // 构建结果
    HashMap<PathResult> res;
    for (int i = 0; i < ids.size(); ++i) {
        PathResult pr;
        double* dv = dist.find(ids[i]);
        if ((pr.reachable = (dv && *dv != INF))) {
            pr.totalTime = *dv;
            pr.totalCost = *cost.find(ids[i]);
            pr.path = tracePath(prev, ids[i]);
        }
        res.set(ids[i], pr);
    }
    return res;
}

// ================================================================
// 两点最低费用（结构同上，以 cost 为主权重）
// ================================================================
PathResult Dijkstra::cheapestPath(const Graph& g, int src, int dst) {
    // ... 同 shortestTimeFrom，但 nd = *dp + e.cost
    // 完整代码见 ExpressRouting/Dijkstra.cpp
}

// ================================================================
// Kahn 拓扑排序（子集版本）
// ================================================================
TopoResult TopoSort::sort(const Graph& g, const DynArray<int>& ids) {
    TopoResult res;
    if (ids.empty()) return res;

    // 1. 建立子集查找表
    HashMap<bool> inSet;
    for (int i = 0; i < ids.size(); ++i) inSet.set(ids[i], true);

    // 2. 统计子集内各节点入度
    HashMap<int> deg;
    for (int i = 0; i < ids.size(); ++i) deg[ids[i]] = 0;
    for (int i = 0; i < ids.size(); ++i)
        for (const Edge& e : g.getNeighbors(ids[i]))
            if (inSet.contains(e.to)) ++deg[e.to];

    // 3. 入度=0 → 入队
    Queue<int> q;
    for (int i = 0; i < ids.size(); ++i)
        if (deg[ids[i]] == 0) q.push(ids[i]);

    // 4. BFS
    while (!q.empty()) {
        int u = q.front(); q.pop();
        res.order.push_back(u);
        for (const Edge& e : g.getNeighbors(u))
            if (inSet.contains(e.to) && --deg[e.to] == 0)
                q.push(e.to);
    }

    // 5. 判环：出队数 < 总数 → 有环
    if (res.order.size() < ids.size()) {
        res.hasCycle = true;
        HashMap<bool> done;
        for (int i = 0; i < res.order.size(); ++i) done.set(res.order[i], true);
        for (int i = 0; i < ids.size(); ++i)
            if (!done.contains(ids[i])) res.cycleNodes.push_back(ids[i]);
    }
    return res;
}
```

### 6.7 测试

```cpp
Graph g;
g.addNode(Node(1, "北京", "...", 116.4, 39.9));
g.addNode(Node(2, "上海", "...", 121.5, 31.2));
g.addNode(Node(3, "广州", "...", 113.3, 23.1));
g.addEdge(Edge(1, 2, 5.0, 300.0));
g.addEdge(Edge(2, 3, 6.0, 350.0));
g.addEdge(Edge(1, 3, 10.0, 600.0));

// 测试最短耗时（北京出发）
auto results = Dijkstra::shortestTimeFrom(g, 1);
PathResult* pr = results.find(3);
cout << "北京→广州 最短耗时: " << pr->totalTime << "h\n";   // 应 ≈ 11h
cout << "路径: ";
for (int i = 0; i < pr->path.size(); ++i) cout << pr->path[i] << " ";
// 应输出 1 2 3（经上海比直达快）

// 测试最低费用
PathResult r = Dijkstra::cheapestPath(g, 1, 3);
cout << "北京→广州 最低费用: " << r.totalCost << "元\n";    // 应 = 600
```

✅ 算法层完成。

---

## 七、OrderManager 订单管理 + 文件读写

### 7.1 数据结构

```
Order:                         DeliveryPlan:
┌──────────────────┐           ┌──────────────────────┐
│ orderId: int     │           │ order: Order          │
│ srcNode: int     │           │ result: PathResult    │
│ dstNode: int     │           └──────────────────────┘
│ goods: string    │
│ byTime: bool     │  ← true=按耗时, false=按费用
└──────────────────┘
```

### 7.2 文件格式

```
network.txt（分段格式）：
  # NODES
  1 北京总仓 北京市朝阳区 116.4 39.9     ← 编号 名称 地址 经度 纬度
  2 上海总仓 上海市浦东新区 121.5 31.2
  # EDGES
  1 2 1.5 60.00                         ← 起点 终点 耗时(h) 费用(元)

orders.txt：
  # 订单号 起点 终点 货物 优化目标(0=费用/1=耗时)
  1001 1 20 电子产品 1
```

### 7.3 核心解析逻辑

节点行 `1 北京总仓 北京市朝阳区 116.4 39.9` 的处理：

```
拆词后的 tokens = ["1", "北京总仓", "北京市朝阳区", "116.4", "39.9"]
  编号 = tokens[0]
  名称 = tokens[1]
  纬度 = tokens[最后-1]     ← 最后两个词
  经度 = tokens[最后-2]
  地址 = tokens[2..最后-3] 拼起来  ← 中间的词（可能含空格）
```

这样即使地址含空格（如 "广西壮族自治区 南宁市"）也能正确解析。

### 7.4 OrderManager.h / OrderManager.cpp

完整代码见 `ExpressRouting/OrderManager.h` 和 `ExpressRouting/OrderManager.cpp`。核心方法：

| 方法 | 功能 |
|------|------|
| `FileManager::loadNetwork()` | 从 txt 读取路网，分段解析 #NODES/#EDGES |
| `FileManager::saveNetwork()` | 保存当前路网到 txt |
| `FileManager::loadOrders()` | 从 txt 读取订单 |
| `FileManager::savePlans()` | 导出配送方案 |
| `OrderManager::planAllOrders()` | 遍历订单，每条调用 Dijkstra |
| `OrderManager::planBatchSequence()` | 收集订单涉及的节点 → 拓扑排序 |

---

## 八、Qt GUI 图形界面

> ⚠️ 这是最复杂的部分（~500 行）。建议在 Qt Creator 中边写边调试。
> 核心思路：**先搭骨架，再加交互**。

### 8.1 界面布局

```
┌─────────────────────────────────────────────────────┐
│ 菜单栏: [文件] 导入路网 | 导出路网 | 导入订单 | 导出方案 │
├───────────────┬─────────────────────────────────────┤
│ 左侧面板(260px)│          画布（占满剩余空间）          │
│ 深蓝背景       │                                     │
│               │    · 蓝色圆 = 城市                    │
│ 统计: 25/60/5 │    · 绿色 = 起点  红色 = 终点         │
│               │    · 橙色 = 路径高亮                  │
│ [网点管理]     │    · 灰线 = 有向边（箭头）             │
│ [路网管理]     │    · 鼠标悬停 → 浮窗详情              │
│ [路径查询]     │    · 鼠标点击 → 状态栏                │
│ [批次配送]     │                                     │
│               │                                     │
│ 操作日志       │                                     │
├───────────────┴─────────────────────────────────────┤
│ 状态栏: [1] 北京总仓  北京市朝阳区                     │
└─────────────────────────────────────────────────────┘
```

导航方式：左侧按钮不是简单网页跳转，而是 **QStackedWidget** 切换页面：
- 主菜单（4 个大按钮）
- 网点管理子页（添加/删除/修改/查询/列表/返回）
- 路网管理子页
- 路径查询子页
- 批次配送子页

### 8.2 画布核心几何

**经纬度 → 屏幕坐标**：

```
找所有节点的 lon/lat max/min
对每个节点：
  x = 边距 + (lon - minLon) / (maxLon - minLon) × 画布宽度
  y = 边距 + 画布高度 - (lat - minLat) / (maxLat - minLat) × 画布高度
  （纬度越大越靠北 → 屏幕 y 越小 → 需要反向）
```

**画箭头**：

```
方向向量 u = (终点 - 起点) 归一化
垂直向量 perp = (-u.y, u.x)

画线：起点圆边缘 → 终点圆边缘（减去圆半径）
箭头三角：在终点处画三个顶点构成的三角形
  - 顶点1: 终点
  - 顶点2: 终点 - u×箭头长 + perp×半宽
  - 顶点3: 终点 - u×箭头长 - perp×半宽

双向边：两条线各偏移 perp×5，避免重叠看不清
```

**鼠标命中**：

```
对每个节点：距离 = √((鼠标x - 圆心x)² + (鼠标y - 圆心y)²)
距离 ≤ 圆半径 + 4px → 命中
```

### 8.3 Qt 信号槽速成

```cpp
// connect("信号发出者", "信号", "接收者", "槽函数")
connect(按钮, &QPushButton::clicked,  this, &MainWindow::槽名);
//          ↑ 谁发               ↑ 发什么     ↑ 谁收      ↑ 收后做啥

// Lambda 也可以做槽：
connect(画布, &GraphWidget::nodeHovered, this, [this](int id) {
    statusBar()->showMessage("悬停节点 " + id);
});
```

### 8.4 MainWindow.h / MainWindow.cpp

完整代码见 `ExpressRouting/MainWindow.h` 和 `ExpressRouting/MainWindow.cpp`。

建议阅读顺序：
1. **先看 `buildUI()`** — 界面怎么搭的
2. **再看 `GraphWidget::paintEvent()`** — 画布怎么渲染的（边→节点→浮窗 三层）
3. **然后看鼠标事件** — 交互怎么实现的
4. **最后看业务槽函数** — 按钮怎么连通到算法

### 8.5 main.cpp

```cpp
#include <QApplication>
#include <QDir>
#include "MainWindow.h"

int main(int argc, char* argv[]) {
    QApplication app(argc, argv);
    QDir::setCurrent(QCoreApplication::applicationDirPath());
    MainWindow win;
    win.show();
    return app.exec();
}
```

---

## 九、数据文件 + 构建配置 + 编译运行

### 9.1 data/network.txt

25 个中国真实城市，124 条有向边，双权（耗时+费用）。参见项目 `data/network.txt`。

### 9.2 data/orders.txt

5 条测试订单，覆盖南北、东西向，不同优化目标。参见项目 `data/orders.txt`。

### 9.3 CMakeLists.txt

```cmake
cmake_minimum_required(VERSION 3.16)
project(ExpressRouting VERSION 1.0 LANGUAGES CXX)

set(CMAKE_AUTOUIC ON)
set(CMAKE_AUTOMOC ON)        # ★ 自动处理 Q_OBJECT 的 moc
set(CMAKE_AUTORCC ON)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

find_package(QT NAMES Qt6 Qt5 REQUIRED COMPONENTS Widgets)
find_package(Qt${QT_VERSION_MAJOR} REQUIRED COMPONENTS Widgets)

set(PROJECT_SOURCES
    main.cpp MainWindow.cpp MainWindow.h
    Graph.cpp Graph.h Dijkstra.cpp Dijkstra.h
    OrderManager.cpp OrderManager.h Containers.h
)

if(${QT_VERSION_MAJOR} GREATER_EQUAL 6)
    qt_add_executable(ExpressRouting MANUAL_FINALIZATION ${PROJECT_SOURCES})
else()
    add_executable(ExpressRouting ${PROJECT_SOURCES})
endif()

target_include_directories(ExpressRouting PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(ExpressRouting PRIVATE Qt${QT_VERSION_MAJOR}::Widgets)

# 编译后自动复制 data/ 到输出目录
add_custom_command(TARGET ExpressRouting POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E copy_directory
        ${CMAKE_CURRENT_SOURCE_DIR}/data $<TARGET_FILE_DIR:ExpressRouting>/data
)

if(QT_VERSION_MAJOR EQUAL 6)
    qt_finalize_executable(ExpressRouting)
endif()
```

### 9.4 编译运行

**Qt Creator（推荐）**：
1. File → Open File or Project → 选 `CMakeLists.txt`
2. 选编译套件（MSVC 或 MinGW）
3. 点左下角 🔨 构建，点 ▶ 运行

**VS Code（备选）**：
项目已提供 `.vscode/tasks.json` 和 `.vscode/launch.json`，但需要手动指定 Qt 头文件路径。

### 9.5 功能测试清单

| # | 测试 | 操作 | 预期 |
|---|------|------|------|
| 1 | 启动 | 直接运行 | 自动加载 network.txt，画布显示 25 个城市 |
| 2 | 悬停 | 鼠标移节点上 | 浮窗显示编号/名称/地址/出边数 |
| 3 | 点击 | 点节点 | 状态栏显示详情 |
| 4 | 最短耗时 | 路径查询→单源→输入 1 | 从北京到各城市的高亮路径 |
| 5 | 最低费用 | 路径查询→两点→1→20 | 北京→广州最便宜路径 |
| 6 | 添加网点 | 网点→添加→输入信息 | 画布出现新节点 |
| 7 | 删网点 | 网点→删除 | 节点和相关边消失 |
| 8 | 批量规划 | 批次→规划所有 | 5 条订单全算出来 |
| 9 | 拓扑排序 | 批次→拓扑排序 | 输出排序序列 |
| 10 | 导出方案 | 批次→导出 | data/plans.txt 生成 |

---

## 十、算法详解附录

### 10.1 Dijkstra 正确性

**定理**：Dijkstra 算法结束时，dist[v] = src→v 的最短距离。

**证明（归纳）**：每次 pop 出 u 时，dist[u] 已是最优。

反证：假设存在更短路径 P：src→...→x→...→u。
- x 已被 pop？→ 松弛 x 的边时就更新了 dist[u]，矛盾
- x 未被 pop？→ dist[x] ≤ dist[P中距离] < dist[u]
  → 堆顶不该 pop u（应该 pop x），矛盾 ✓

### 10.2 惰性删除复杂度

```
总 push：≤ E 次（每条边最多松弛一次）
总 pop：≤ E 次（包含跳过旧记录的操作）
每次 push/pop：O(log E)
总复杂度：O((V+E) log E) ≈ O((V+E) log V)
```

### 10.3 Kahn 拓扑排序

**定理**：如果所有节点出队，出队顺序是合法拓扑序。

**证明**：
- 节点 v 入队时，其所有在子集内的前驱都已出队
- 出队顺序：前驱在前 → 边 u→v，u 在 v 前 ✓
- 有环 → 环节点入度永远 > 0 → 出队数 < 总数

### 10.4 复杂度总表

| 操作 | 复杂度 |
|------|--------|
| DynArray push_back | 均摊 O(1) |
| DynArray erase | O(n) |
| HashMap insert/find | 均摊 O(1) |
| MinHeap push/pop | O(log n) |
| Queue push/pop | O(1) |
| Graph addNode | O(1) |
| Graph deleteNode | O(E) |
| Graph addEdge | O(degree) |
| Dijkstra (堆优化) | O((V+E)log V) |
| Kahn 拓扑排序 | O(V+E) |

---

## 十一、答辩准备

### 11.1 老师最可能问的 5 个问题

**Q1："Dijkstra 为什么用堆？"**

> 堆优化把"选最小 dist 节点"从 O(V) 降到 O(logV)。虽然 25 节点时差异不大（朴素 625 vs 堆 685），但展示了算法优化的思路。惰性删除避免了实现 decrease-key 的复杂度，每条边 push 一次，旧记录 pop 时跳过。

**Q2："HashMap 为什么用开放地址法而不是链地址法？"**

> 开放地址法所有数据在连续内存上，缓存友好。不需要额外链表节点分配。负载因子控制在 0.5，保证探测链 ≤ 2 次。惰性删除用 del 标记，插入时可复用。哈希用乘法哈希（黄金比例倒数），对连续整数分布均匀。

**Q3："邻接表 vs 邻接矩阵？"**

> 本项目 25 节点 124 边，密度 ~20%，是稀疏图。邻接表 O(V+E) 空间，HashMap find O(1)，遍历出边 O(degree)。邻接矩阵 O(V²) 空间且扫全行，对稀疏图不利。另外 HashMap 的自实现本身也是课程要求。

**Q4："拓扑排序有环怎么处理？"**

> Kahn 算法结束后出队数 < 子集节点数 → 有环。我收集所有入度 > 0 的节点作为"环节点列表"，在 GUI 标红显示。用户不仅知道有环，还能看到哪些节点在环里。

**Q5："双权边怎么处理？"**

> 每条边存 time 和 cost。Dijkstra 代码只写一份，最短耗时时以 time 为比较权重（cost 顺带算），最低费用时以 cost 为比较权重（time 顺带算）。两函数结构完全相同，只改了一行比较语句。

### 11.2 三句话讲清核心

- **Dijkstra**：①MinHeap 取最短节点 ②惰性删除跳旧记录 ③prev 回溯 + 翻转得路径
- **拓扑排序**：①统计入度 ②0 入队 ③出队减度 ④0 继续入队 ⑤比数量判环
- **HashMap**：①乘法哈希定位 ②线性探测冲突 ③惰性删除打标记

### 11.3 代码行数

| 层 | 文件 | 行数 |
|----|------|------|
| 容器 | Containers.h | ~260 |
| 图 | Graph.h + .cpp | ~150 |
| 算法 | Dijkstra.h + .cpp | ~150 |
| 业务 | OrderManager.h + .cpp | ~200 |
| GUI | MainWindow.h + .cpp | ~500 |
| 入口 | main.cpp | ~20 |
| 构建 | CMakeLists.txt | ~50 |
| 数据 | network.txt + orders.txt | ~95 |
| **合计** | | **~1425** |

---

*文档版本 v2.0 | 匹配简化后代码 | 2026-06-10*
